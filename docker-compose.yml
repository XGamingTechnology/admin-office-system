# docker-compose.yml
name: office-admin-system

services:
  # 🗄️ PostgreSQL Database
  db:
    image: postgres:16-alpine
    container_name: office_admin_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - db_data:/var/lib/postgresql/data
      - ./backups:/backups
    networks:
      - office_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER}"]
      interval: 10s
      timeout: 5s
      retries: 5

  # 🔌 Backend NestJS
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: office_admin_backend
    restart: unless-stopped
    environment:
      - POSTGRES_HOST=db
      - POSTGRES_PORT=5432
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - JWT_SECRET=${JWT_SECRET}
      - JWT_EXPIRES_IN=${JWT_EXPIRES_IN}
      - NODE_ENV=production
      - PORT=3000
      - CORS_ORIGIN=https://office.getopurtunity.online
    ports:
      - "127.0.0.1:8080:3000" # 🔒 Hanya localhost, akses via Nginx proxy
    depends_on:
      db:
        condition: service_healthy
    networks:
      - office_network
    command: sh -c "sleep 5 && node dist/main"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/api/office/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  # 🎨 Frontend React + Vite + Nginx
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        - VITE_API_URL=${VITE_API_URL:-https://office.getopurtunity.online/api/office}
    container_name: office_admin_frontend
    restart: unless-stopped
    environment:
      - VITE_API_URL=https://office.getopurtunity.online/api/office
    ports:
      - "127.0.0.1:5173:80" # 🔒 Hanya localhost, akses via Nginx proxy
    depends_on:
      - backend
    networks:
      - office_network

volumes:
  db_data:

networks:
  office_network:
    driver: bridge
